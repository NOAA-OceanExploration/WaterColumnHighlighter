#!/bin/bash

# Install PyTorch
pip install torch torchvision

# Install OpenCV
pip install opencv-python

# Install pandas
pip install pandas

# Install NumPy
pip install numpy

# Install Boto3 (for S3 interaction)
pip install boto3

# Install scikit-learn
pip install scikit-learn

# Install toml (for configuration)
pip install toml

# Install Weights & Biases (wandb)
pip install wandb

# Install tarfile (for tar archive extraction)
pip install tarfile

# Install datetime (for timestamp handling)
pip install datetime